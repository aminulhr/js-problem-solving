// celsius to fahrenheit

function temperature(cel) {
  let far = (cel * 9) / 5 + 32;
  return far;
}
console.log(temperature(5)); // or

//------------------------------------------------

function temperature2(cel2) {
  return (cel2 * 9) / 5 + 32;
}
console.log(temperature2(5));
